def avDigits():
    global num
    global average
    global digits
    while num > 0:
        digits += num % 10
        average = len(digits)






digits=0
average=0
number=int(input("Enter the number you want:"))
num=number
